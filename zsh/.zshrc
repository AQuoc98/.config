# Warp
[ -f "$HOME/.config/zsh/warp.zsh" ] && source "$HOME/.config/zsh/warp.zsh"

# Custom zsh
# [ -f "$HOME/.config/zsh/custom.zsh" ] && source "$HOME/.config/zsh/custom.zsh"

# Aliases
# [ -f "$HOME/.config/zsh/aliases.zsh" ] && source "$HOME/.config/zsh/aliases.zsh"
